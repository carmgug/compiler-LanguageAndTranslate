package compiler.Lexer.SpeciefiedSymbol;

import compiler.Lexer.Symbol;
import compiler.Lexer.Token;

public class BaseType extends Symbol {
    public BaseType(Token type, String e, int n_line) {
        super(type, e, n_line);
    }
}
